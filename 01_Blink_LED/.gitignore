# Thư mục build (tự động tạo lại, không cần đưa lên GitHub)
Debug/
Release/

# File IDE (không bắt buộc, nhưng nếu muốn người khác mở lại project dễ dàng thì giữ .cproject và .project)
.cproject
.project
.settings/

# File tạm hệ điều hành
*.log
*.tmp
*.swp

# File biên dịch trung gian
*.o
*.elf
*.bin
*.map
*.hex

# Thư mục CubeMX scratch (nếu có)
mx.scratch/

# CubeMX file (nếu muốn giữ thì comment dòng này lại)
# *.ioc
